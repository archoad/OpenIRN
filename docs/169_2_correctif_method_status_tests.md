# Patch 169.2 — Correctif `methodStatus` dans les tests Flutter

Ce micro-patch aligne les tests Flutter sur la nouvelle grille de notation IRN introduite par le patch 169.

L'ancien identifiant de méthode `public_rnr_unweighted` est remplacé par `irn_scale_unweighted_v1` dans les assertions de tests.

Aucun changement fonctionnel applicatif.
